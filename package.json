{
  "name": "amis-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "AMIS (Adolescent Mother Information System)",
  "scripts": {
    "audit:high": "npm audit --audit-level=high",
    "build": "node scripts/build.js",
    "deploy:check": "npm run audit:high && npm run build",
    "dev": "node scripts/dev-server.js",
    "prevercel:dev": "npm run audit:high",
    "vercel:dev": "npx vercel dev"
  },
  "dependencies": {
    "googleapis": "^173.0.0"
  }
}
